apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: minio
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'minio-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/wintech-thai/please-payment-k3s-pjp2.git
        targetRevision: "{{name}}" #Based on environment
        path: '99-deployments/manifests/minio'
      destination:
        name: in-cluster
        namespace: minio
      syncPolicy:
        automated:
          selfHeal: false
          prune: true
        syncOptions:
          - CreateNamespace=true
